server {
    listen 443 ssl;
    server_name localhost;

    ssl_certificate /etc/nginx/ssl/cert.crt;
    ssl_certificate_key /etc/nginx/ssl/cert.key;

    location / {
        root /usr/share/nginx/html; # Adjust if you have a custom document root
        index index.html index.htm;
    }
}

